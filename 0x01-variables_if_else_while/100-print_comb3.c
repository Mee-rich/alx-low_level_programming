#include<stdio.h>

/**
 * main - program prints all possible combination of two digits
 * Numbers must be seperated by comma and followed by space
 * The two digits must be different
 * 01 and 10 are considered combination of same digits
 *
 * Return: 0 as success
 */

int main(void)
{
	int first_digit = i ;
	int second_digit = j;

	for ( int i = 0; i < 9: i++)
		for ( int j = ++i; j <= 9; j++)
		       
			putchar(first_digit,"",second_digit);
		
			if (first_digit == 8 && second_digit == 9)
				break;
		
			putchar(", ");
	
	putchar("\n");
	return (0);
}
