#include<stdio.h>

/**
 * main - program prints all possible outcomes of three different digits
 *
 * Return: 0
 */

int main(void)
{
	int firstNum = i;
	int secondNum = j;
	int thirdNum = k;

	for (i = 0; i < 8; i++)
	       for (j = ++i; j < 9; j++)
		       for (k = ++j; k < 10; k++)
			       
			putchar(i,"",j,""k)
			
			if ( i == 7 && j == 8 && k ==9)
				break;
}
