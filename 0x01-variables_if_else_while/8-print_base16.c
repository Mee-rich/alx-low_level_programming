#include<stdio.h>

/**
 * main - function to print base 16 in lowercase
 *
 * Return: 0
 */

int main(void)
{
	int n = 0;
	int l = 'a';
	
	while (n < 10)
		putcahar("%d", n++)

	while (l < 'f')
		putchar("%d", l++);
	putchar("\n");

	return (0);
}
